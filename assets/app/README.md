# /app assets

Everything here is real product material — screen recordings and screenshots
from the shipping iOS app, plus the in-app mascot. Nothing is a placeholder or
stock.

## Where each file appears

| File | Section | Source |
| --- | --- | --- |
| `hero-video.mp4` + `hero-poster.webp/.jpg` | Hero phone frame | 14s cut from the 2-minute screen recording (t≈61–75): "Calling Chef" → missing items → Sear Beef Fillet → technique clip |
| `voice-cookmode-*.webp` | Glasses section, left | IMG_7239 — dark cook mode, "Say Chef if you need anything" |
| `glasses-toggle-*.webp` | Glasses section, right | IMG_7226 — Skills screen with the Meta glasses toggle |
| `cookmode-video-*.webp` | Proof tile 1 | IMG_7238 — cook mode playing a technique clip |
| `chefs-row-*.webp` | Proof tile 2 | IMG_7231 — Gordon Ramsay profile |
| `ask-polly-*.webp` | Proof tile 3 | IMG_7236 — Ask Polly chat |
| `grocery-list-*.webp` | Proof tile 4 | IMG_7230 — auto grocery list |
| `discover-feed-*.webp` | How it works, step 1 | IMG_7224 |
| `recipe-detail-*.webp` | How it works, step 2 | IMG_7235 — Beef Wellington |
| `skills-lesson-*.webp` | How it works, step 3 | IMG_7227 — knife skills |
| `bear-chef.webp` | Hero + final CTA | Mascot lifted from IMG_7226 |

Also converted and available but not currently placed:
`recipes-home`, `kitchen-pantry`, `kitchen-tools`, `glasses-watch`.

## Video

444×900, 14s, no audio track, `moov` at the front, 474KB. The status bar was
cropped out because the recording carries a red capture indicator. WebM was
built and dropped — it encoded larger than the MP4, so it would only have been
dead weight.

## Still needed from you

**Review quotes and a star rating.** The App Store currently reports
`averageUserRating: 0` and `userRatingCount: 0`, and the reviews feed is empty,
so the social-proof section was left out rather than filled with invented
praise. Once real reviews exist, drop them in and re-enable that section — the
markup is in the page history, or ask and it takes a minute to add.

A screenshot showing a **cooking streak** would also strengthen that section;
none of the thirteen supplied screenshots has one.
